import { Enumeration } from "../../standard/index.js";

export const MouseButton = Enumeration.create({
    Left: 0,
    Wheel: null,
    Right: null,
    BrowserBack: null,
    BrowserNext: null,
});